module.exports = {
  // Use single quotes instead of double quotes
  singleQuote: true,

  // No semicolons (Vue/JS convention)
  semi: false,

  // 2 spaces for indentation
  tabWidth: 2,

  // Use spaces instead of tabs
  useTabs: false,

  // Trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'es5',

  // Print width - wrap lines at 100 characters
  printWidth: 100,

  // Add spaces inside object braces: { foo: bar }
  bracketSpacing: true,

  // Put > of multi-line HTML elements at end of last line
  bracketSameLine: false,

  // Arrow function parentheses: always include
  arrowParens: 'always',

  // Line endings: auto-detect (LF on Unix, CRLF on Windows)
  endOfLine: 'auto',

  // Vue-specific: Indent script and style tags
  vueIndentScriptAndStyle: false,
}
